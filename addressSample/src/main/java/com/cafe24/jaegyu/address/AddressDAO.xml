<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cafe24.jaegyu.address.AddressDAO">

	<!-- <insert id="insert" parameterType="org.thinker.sample.member.MemberVO"> 
		insert into tbl_member (userid,userpw,username,email) values (#{id},#{pw},#{userName},#{email}) 
		</insert> -->

	<select id="getAll" resultType="com.cafe24.jaegyu.address.AddressVO">
		select *
		from address;
	</select>

	<update id="update">
		update address set address=#{address}, name=#{name},
		tel=#{tel} where id=#{id}
	</update>

</mapper>